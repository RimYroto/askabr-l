# Pinned build dependencies for Windows onefile (Python 3.14, CPU).
# Install torch/torchvision from PyTorch CPU index in build_windows.py before this file.

pyinstaller>=6.10.0
pyinstaller-hooks-contrib>=2025.4
PyYAML>=6.0.1
Pillow>=10.0.0
numpy>=2.4.0
PyQt6>=6.6.0
certifi>=2024.0.0
